generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./datos.db"
}

model Tarea {
  id       Int     @id @default(autoincrement())
  proyecto String
  titulo   String
  hecha    Boolean @default(false)
}
