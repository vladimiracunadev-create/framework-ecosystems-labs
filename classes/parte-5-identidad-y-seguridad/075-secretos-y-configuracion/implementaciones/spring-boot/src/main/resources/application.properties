# Spring mapea variables de entorno a propiedades: APP_ENTORNO llega como
# app.entorno. Sin valor por omision — el `:` vacio dejaria arrancar con el
# secreto sin poner, que es justo lo que la clase ensena a no hacer.
app.entorno=${APP_ENTORNO:}
app.secreto=${APP_SECRETO:}
