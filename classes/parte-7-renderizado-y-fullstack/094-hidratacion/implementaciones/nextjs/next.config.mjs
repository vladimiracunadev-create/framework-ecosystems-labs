/**
 * Sin configuración.
 *
 * En Next.js la hidratación no se activa ni se desactiva aquí. Se decide
 * archivo por archivo con la directiva `"use client"`: lo que la lleve —y todo
 * lo que ese archivo importe— viaja al navegador y se hidrata. Lo demás se
 * ejecuta en el servidor y se queda en el servidor.
 */
export default {};
