/**
 * Sin configuración.
 *
 * Next no llama islas a esto y el matiz importa: su modelo son componentes de
 * servidor con fronteras de cliente. El efecto se parece —solo viaja el código
 * de lo interactivo— pero el mecanismo es otro, y `/islas.json` publica la
 * diferencia que se puede medir.
 */
export default {};
