/**
 * Sin configuración de estrategia.
 *
 * En Next.js la estrategia NO se elige aquí: se elige en cada ruta, con
 * `export const dynamic` o con las opciones de `fetch`. Este archivo existe casi
 * vacío a propósito, para que se vea que la decisión vive al lado de la pantalla
 * y no en un ajuste global.
 */
export default {};
