/**
 * Sin configuración.
 *
 * Los componentes de servidor no se encienden aquí: en el App Router **todo
 * componente lo es** salvo que su archivo empiece por `"use client"`. La
 * configuración vacía es, otra vez, la forma de enseñar que la decisión vive en
 * los archivos y no en un ajuste global.
 */
export default {};
