/**
 * Sin reglas de ruta: aquí todo se renderiza en el servidor en cada petición,
 * que es lo que hace falta para medir cuándo empieza la carga.
 */
export default defineNuxtConfig({
  compatibilityDate: "2025-01-01",
});
