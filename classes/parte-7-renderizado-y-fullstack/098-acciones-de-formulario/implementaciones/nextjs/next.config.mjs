/**
 * Sin configuración. En el App Router la carga de datos no se declara en ningún
 * sitio: la página es una función `async` y espera lo que necesite. Es la
 * propuesta más simple de las cinco y la que menos ceremonia pide.
 */
export default {};
