/**
 * Sin nada especial. Lo que hay que mirar en esta implementación no está aquí:
 * está en `server/routes/tareas.post.ts`, que es la ruta que hubo que escribir a
 * mano porque Nuxt no tiene acciones de formulario.
 */
export default defineNuxtConfig({
  compatibilityDate: "2025-01-01",
});
